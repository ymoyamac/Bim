import Bim from "../Bim"

describe("Describe Bim class instance", () => {
    // function getBimInstance(fn: jest.Mock<any, any>) {
    //     return fn(new Bim<any>({
    //         provider: "postgres",
    //         connection: {
    //             host: "localhost",
    //             port: 5432,
    //             user: "ymoy",
    //             password: "root",
    //             database: "test_guarderia"
    //         }
    //     }))
    // }
    // test("Should be class instance of Bim", () => {
    //     const mock = jest.fn()
    //     getBimInstance(mock)
    //     expect(mock).toHaveBeenCalledWith(expect.any(Bim));
    // })
    // test("Should be 5432 the value of port", () => {
    //     const bim = new Bim<any>({
    //         provider: "postgres",
    //         connection: {
    //             host: "localhost",
    //             port: 5432,
    //             user: "ymoy",
    //             password: "root",
    //             database: "test_guarderia"
    //         }
    //     })
    //     expect(bim.getPort()).toBe(5432)
    // })
 })